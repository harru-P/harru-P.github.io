var text = document.getElementById("textbox");
var button = document.getElementById("button");

function on(){
    if(text.value == "harru"){
        window.location = "media/hidden.txt"
    }
}